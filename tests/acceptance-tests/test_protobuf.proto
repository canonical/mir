option cc_generic_services = true;

package mir.protobuf;

message Parameters {
  required string name = 1;
}


message Result {
  optional string error = 127;
  optional string value = 1;
}

service MirServer {
  rpc function(Parameters) returns (Result);
}

