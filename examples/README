examples for mir clients. you should have package 'libmirclient-dev' installed

you can compile with a command like:
    g++ -std=c++0x -o mir_demo_client_accelerated `pkg-config --libs --cflags mirclient` demo_client_accelerated.cpp graphics_utils.cpp
    gcc -o mir_demo_client_unaccelerated `pkg-config --libs --cflags mirclient` demo_client_unaccelerated.c
