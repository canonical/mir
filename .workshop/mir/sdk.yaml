name: mir
plugs:
  desktop:
    interface: desktop
  build:
    interface: mount
    workshop-target: /home/workshop/build
  ccache:
    interface: mount
    workshop-target: /home/workshop/.cache/ccache
  wlcs-build:
    interface: mount
    workshop-target: /home/workshop/wlcs-build
  gpu:
    interface: gpu
